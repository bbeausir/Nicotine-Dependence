/**
 * TODO(DATA): Create Supabase project, add EXPO_PUBLIC_SUPABASE_URL + EXPO_PUBLIC_SUPABASE_ANON_KEY,
 * then implement createClient and typed database types.
 *
 * import { createClient } from '@supabase/supabase-js';
 */

export function getSupabaseClient(): null {
  return null;
}
